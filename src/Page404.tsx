import React, { ReactElement } from 'react'

interface Props {
    
}

export default function Page404({}: Props): ReactElement {
    return (
        <div>
            Not found... :(
        </div>
    )
}
